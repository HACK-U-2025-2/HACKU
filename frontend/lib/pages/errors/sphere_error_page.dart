import 'package:flutter/material.dart';

class SphereErrorPage extends StatelessWidget {
  const SphereErrorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('思考空間の表示に失敗しました')));
  }
}
